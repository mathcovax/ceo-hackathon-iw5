import {
	mdiClose,
	mdiAccount,
	mdiRobot,
} from "@mdi/js";

export const iconsMapper = {
	close: mdiClose,
	account: mdiAccount,
	robot: mdiRobot,
};

export const iconSizeMapper = {
	small: 14,
	default: 24,
	large: 36,
};
